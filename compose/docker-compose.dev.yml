version: '3.9'

services:
  ghost:
    environment:
      NODE_ENV: 'development'
  sleepymaid:
    environment:
      NODE_ENV: 'dev'
  helper:
    environment:
      NODE_ENV: 'dev'
  website:
    environment:
      NODE_ENV: 'development'
  postgres:
    ports:
      - '127.0.0.1:5433:5432'
